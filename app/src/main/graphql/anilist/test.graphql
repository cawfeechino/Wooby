query test($genre: [String!], $score: Int!, $sort: [MediaSort!]) {
  Page {
    media(type: ANIME, genre_in: $genre, averageScore_greater: $score, sort:$sort) {
      id
      title {romaji, english}
      averageScore
      description
      genre
    }
  }
}